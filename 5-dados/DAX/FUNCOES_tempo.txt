CALENDAR(MIN(DATA MINIMA), MAX(DATA MAXIMA))
YEAR(DATA) retorna o ano daquela data
MONTH()
DAY()
FORMAT(DATA, "MMM") mes po extenso
FORMAT(DATA, "q") mostra o trimestre
FIRSTDATE(DATA)
LASTDATE(DATA)
STARTOFMONTH(DATA)mostra o primeiro dia de cada mês
ENDOFMONTH()
CALCULATE(SUM(VENDAS), DATEADD(DATA, 1, DAY) mostra o valor do dia seguinte
CALCULATE(SUM(VENDAS), NEXTDAY(DATA))
CALCULATE(SUM(VENDAS), NEXTMONTH(DATA))
CALCULATE(SUM(VENDAS), NEXTQUARTER(DATA))
CALCULATE(SUM(VENDAS), NEXTYEAR(DATA))
CALCULATE(SUM(VENDAS), DATESMTD(DATA)) acumulativo por mês
CALCULATE(SUM(VENDAS), DATESQTD(DATA)) acumulativo por trimestre
CALCULATE(SUM(VENDAS), DATESYTD(DATA)) acumulativo por ano
CALCULATE(SUM(VENDAS), PREVIOUSDAY(DATA)) mostra o valor do dia anterior
CALCULATE(SUM(VENDAS), PREVIOUSMONTH(DATA)) mostra o valor do MES anterior
CALCULATE(SUM(VENDAS), PREVIOUSYEAR(DATA)) mostra o valor do ANO anterior
CALCULATE(SUM(VENDAS), DATESBETWEEN(DATA, DATE(2022, 01,01), DATE(2022, 12, 31)))

